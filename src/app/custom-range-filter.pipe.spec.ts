import { CustomRangeFilterPipe } from './custom-range-filter.pipe';

describe('CustomRangeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomRangeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
